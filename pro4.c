#include<stdio.h>
int main()
// -: find the area of cube:-
{
    float a,A;
    printf("Enter the length of rectangle:");
    scanf("%f",&a);
    A=a*a*a;
    printf("The required Area of cube is:%f",A);
    return 0;
}